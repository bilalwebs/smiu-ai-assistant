name: Frontend CI

on:
  push:
    branches: [main]
  pull_request:

# Placeholder pipeline for the Next.js frontend.
# Add steps: install deps, lint, type-check, build, test.

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Frontend CI - configure lint, type-check and tests"
