# FastAPI backend service. Copy to .env and adjust values.

ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=DEBUG

APP_NAME=smiu-ai-assistant-backend
VERSION=0.1.0
API_VERSION=1.0

HOST=0.0.0.0
PORT=8000

# SQLite for local development. PostgreSQL in production:
# DATABASE_URL=postgresql+psycopg://smiu:smiu@localhost:5432/smiu_assistant
DATABASE_URL=sqlite+aiosqlite:///./data/dev.db

# Connection pool (PostgreSQL only)
DB_ECHO=false
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=1800

# CORS (comma-separated list of allowed frontend origins)
CORS_ORIGINS=http://localhost:3000

# Request / correlation id headers
REQUEST_ID_HEADER=X-Request-Id
CORRELATION_ID_HEADER=X-Correlation-Id

# OpenAPI docs (enabled by default; disabled automatically in production)
# DOCS_URL=/docs
# REDOC_URL=/redoc
# OPENAPI_URL=/openapi.json
